interface:
  display_name: "PoDM-BMC Compare"
  short_description: "One-step PoDM/BMC compare"
  default_prompt: "Use $podm-bmc-compare 20260609 to run the PoDManager/BMC compare script; it auto-extracts interface YAML if missing and compares with the PoDM-BMC baseline."
